#include <iostream>
#include<fstream>
#include <sstream>
#include<windns.h>
using namespace std;
class Airline{
    private:
        string destination;
        string flight;
        string departure;
        int  seats;
    public:
         Airline(string dep, string f, string des): destination(dep), departure(dep), flight(f) {};
            
};
int main() {
    

    
    return 0;
}